import { render } from "preact";
import { getRootEl, initClient } from "river.now/client";
import { RiverRootOutlet } from "river.now/{{.UIVariant}}";
import { riverAppConfig } from "./river.gen.ts";
{{.TailwindFileImport}}
await initClient({
	riverAppConfig,
	renderFn: () => {
		render(<RiverRootOutlet />, getRootEl());
	},
});
