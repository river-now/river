package main

import (
	"{{.GoImportBase}}/backend/src/router"

	"github.com/river-now/river"
)

func main() {
	router.App.BuildRiver(river.BuildRiverOptions{
		LoadersRouter: router.LoadersRouter,
		ActionsRouter: router.ActionsRouter,
	})
}
